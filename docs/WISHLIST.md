# Wishlist

1. the organise functionality needs to be interactive, language-based, and semantically coherent, then it shows what the results would be and allow for changes, until the user is happy and triggers it.
2. it should be undoable
3. there should be reminders everywhere (unless the user disables them) to backup everything before any action
4. it would be cool if we can trigger backups through macos
5. we could offer integration to commercial backup services?

# shim to expose pip's vendored pkg_resources as top-level pkg_resources
try:
    from pip._vendor.pkg_resources import *  # type: ignore
except Exception:
    # fallback: try setuptools vendored resource
    try:
        from setuptools import _vendor as _sv
        from _sv.pkg_resources import *  # type: ignore
    except Exception:
        raise
